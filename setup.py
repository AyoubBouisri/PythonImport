from setuptools import setup

setup(
    name='YNAB_MANAGER',
    version='1.0',
    author='Sylvain',
    description='YNAB Manager',
    python_requires='>=3.7, <4',
    packages=['ynab_manager'],
    install_requires=[
    ],
)
